* In your project folder, do the following. This will create a new folder called VENV

```bash
python -m venv VENV
```

* Activate the environment
```bash
source VENV/bin/activate
 ```
 
* Install the requirements
```
pip install -r requirements.txt
```
